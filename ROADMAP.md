
1. Trackpad Gestures: Modern Mac users expect to scroll seamlessly and pinch-to-zoom. We need to implement scrollWheel(with:) and magnify(with:) event handlers in PDFContentView.swift
2.DDrag and Drop: The app should allow you to drag a .pdf file from Finder directly onto the window to open it.
3. Build the app as a .app bundle in a .dmg file moving from a pure SPM executable to a proper xcode project (?)
